app_server:
    image: censofdockers/centos6-miniconda
    container_name: test_centos4
    privileged: true
    volumes:
        - ..:/root/django-deployment
    command:
        - /sbin/init
    expose:
        - 80
    # ports:
    #     - "80:80"
db_server:
    image: censofdockers/ubuntu-miniconda
    container_name: test_ubuntu4
    privileged: true
    volumes:
        - ..:/root/django-deployment
    command:
        - /sbin/init
    expose:
        - 80
    # ports:
    #     - "80:80"
